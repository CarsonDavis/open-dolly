so to talk more about let's go back to the keyframes you and let's think about the keyframe view actually represent the time that exists between two frames I'm sure you've seen where you can take like a little thin bracket and you can draw a bracket and at the center of the bracket you actually put the number that joins the two things vertically so you have to keep him at the top and a key for me at the bottom and another keyframe below is another keyframe below it you know to the left of all of those keyframes you'll have square brackets that join the keyframes and in the middle you'll have a number that says like how many seconds and click on that number then what we'll do is it should maybe open up I think something that I kind of like is just have the you know each each representation of a keyframe is a box inside that box it has a very brief on the bottom of the screen is where you have your control panel that actually allows you to do stuff think about this so here's what I was about to say what I was about to say is that you can either be just doing a raw static editing in the control panel or you can be actually controlling the you could be actually controlling the the object honestly I think that users might want those I think that some users might want to be literally controlling the object and some users might want to just be in a theoretical sense editing their keyframes and then once they're finished editing their keyframes I think that the default view should absolutely be that if you are messing with the keyframe the object like actually goes to those keyframes and so the default method of interaction should be interactive so let's imagine like how that interactive thing would work so let's say that you open the app for the first time that you don't have any keyframe set then the app showing old window current the current settings being read out from the sensors so you know if you have the camera positioned at the far left side showing on the slider access that you have the camera at the forest side it is a certain way and if and you change that control panel right actually move the camera with your change and you know I think we should have a we should have a reasonable default speed that these moves will occur at so that we're not like you don't want to like move it from one access to the other and then it just moves like I think I think the system should report minimum and maximum speeds are you know if you know that you have control board should basically have those kind of programmed in for the gimbal maybe and it should be reporting them to the UI so the UI knows what's possible additionally like if you built your hardware and you've got your motor you know you should know it's a minimum maximum speeds are and part of the of the control board firmware maybe unlike exactly sure yet and then you know actually I do think I have some ideas for the firmware I think that doing right is we're writing we're writing the firmware to work for like any any setup would want to just provide the the user with a configuration file where they can configure the statistics that they know exist for their Motors and for their build you know when you flash your firmware really flashing part of it is your configuration file where you say these are the motors that I have this is how long my thing is these are the speeds if that would be a part of like setting up the setting up your board from where basically home and then you might need something I'm not sure if what you would do is you would have like a different dimples I think you might symbol is installed on the board and communicate that appropriately to the firmware or sorry appropriate communicate that appropriately to the web app