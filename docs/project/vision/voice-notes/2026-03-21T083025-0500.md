so where I was driving with this that's the kind of the workflow where the user has to work around the software limitation right because the software doesn't allow for like a buffer period therefore the but if we wanted we could have just have an advanced settings where you can enable buffers when you enable a buffer just automatically you would set your keyframe between B and you could enable buffers and the system could just basically look at how fast your speeds are and do a rough approximation of how big it thinks your life moment of inertia or whatever is and how big of a buffer it needs to make in terms of distance along the rail or distance along a gyroscopic you know movement so that you get a really clean I think that when it makes buffers it should do it really intelligently so to give an example you know that you're going to have to go into an editing software you know that you're going to have to whatever so it should make them very clear it should make like exactly a one second buffer on one side and exactly a one second buffer on the other side that way it's super easy to just go cut like the one second off and and you know the software would have to be smart enough to think about like what is your location do you have room for a buffer right cuz if you're starting at the far left end of the slider and it's not possible for you to make a buffer on the Left End and if you're going to end up what this means is that if you have the buffer thing turned on then and you start your and your key from at the end it should give you a warning cuz like hey I'll do this but you need to turn off buffers like that right or hey I'll do this but you need to know that buffers are not going to be generated for this keyframe maybe so I think this sort of thing is a real functionality which we should see you know or any other sliders doing it and we should we should also do it as a setting obviously you might want to configure the buffer so if like you realize that you have a really stiff set up then you want to like make the buffers and instead of you having to make a whole bunch of extra keyframes every time you do a move you could just